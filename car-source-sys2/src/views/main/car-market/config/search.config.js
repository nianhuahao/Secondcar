export const rules1 = {
  StartKey: [{ required: true, message: '最低价格', trigger: 'blur' }],
  EndKey: [{ required: true, message: '最高价格', trigger: 'blur' }]
}

export const rules2 = {
  CarId: [{ required: true, message: '车架号', trigger: 'blur' }]
}

export const rules3 = {
  CarBrand: [{ required: true, message: '车品牌', trigger: 'blur' }]
}
